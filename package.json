{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"no build step\"",
    "lint": "eslint . || echo no-eslint",
    "test": "vitest run || jest --ci || echo no-tests"
  }
}
